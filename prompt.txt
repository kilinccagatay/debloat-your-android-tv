Help me tidy up my Android TV over ADB and set up Projectivy Launcher. My goal is a simpler Home screen with fewer promotional rows and fewer unused apps running in the background. Carry out the steps you can, explaining them in everyday language.

MY DEVICE
- Brand and model: [TV BRAND AND MODEL]
- Android TV / Google TV version: [ANDROID VERSION OR UNKNOWN]
- IP address and connection port, if shown: [IP ADDRESS:PORT]

Developer options and debugging are enabled. The TV and computer are on the same trusted network.

BOUNDARIES
Keep this reversible: disable reviewed apps, never uninstall them or clear their data. Do not root, unlock the bootloader, flash firmware or factory-reset the TV. Leave packages enabled when their purpose or dependencies are unclear.

Protect Inputs/HDMI, antenna services, remote controls, sound, keyboard, network connectivity, Google Play Services and Store, DRM/streaming, Settings, System UI, updates and recovery. Keep the stock Home launcher working until Projectivy is verified. Identify the relevant services for this particular model instead of copying another brand's package list.

WORK THROUGH THESE STEPS

1. Connect to the TV
Detect my computer's operating system and check for Google's official Android SDK Platform Tools. Install them if needed. Use the connection address displayed by my TV; do not assume port 5555. If pairing is required, guide me through "Pair device with pairing code" and use its separate pairing port with adb pair before adb connect. Wait for me to approve the TV's authorization prompt.

2. Save a short starting snapshot
Create a local tv-cleanup folder. Save the output of:
adb shell dumpsys meminfo
adb shell pm list packages -s --user 0
adb shell pm list packages -d --user 0

Keep the original files as the before snapshot. List other installed apps only if needed for review. Skip unsupported commands and note that limitation. Do not run benchmarks, storage tests or app-launch timing experiments.

3. Show me what can change
Present a table with the exact package name, its purpose, group and reason:
- Cleanup candidates: verified nonessential apps I do not use, such as unused streaming apps or demos.
- My choice: apps or features whose removal from use depends on my preferences; ask me about these.
- Keep enabled: essential services and anything uncertain.

Check unfamiliar packages using reliable information for my model. Screensavers, voice assistants, setup services and telemetry need individual review, not an automatic "junk" label. Get my approval for specific cleanup candidates and start with those. Resolve the second group's questions before proposing further changes.

4. Disable approved apps
Use one reviewed package per command:
adb shell pm disable-user --user 0 <package>

Work in batches of at most five. After each batch, stop while I test Home, Inputs/HDMI, remote buttons, sound, keyboard and my usual streaming apps. Continue only after I confirm they work.

Keep disabled-packages.txt for packages successfully disabled during this session, and CHANGELOG.md for the reason, original state, result and undo command. Do not include packages that were already disabled. For a package that was previously enabled, use:
adb shell pm enable --user 0 <package>

If something breaks, restore the latest batch first and test again.

5. Install and configure Projectivy
Help me install Projectivy Launcher from Google Play or its official distribution source. Open it and arrange Apps, Continue Watching and supported Inputs shortcuts to my preference. Explain any optional accessibility permission before I choose to grant it.

Use the supported Home-screen selection method for my TV. Test Projectivy, app launching and the Home button before proposing any change to the stock launcher. If disabling that launcher is necessary, get my approval, record its original Home component and recovery commands, and keep ADB connected while testing.

6. Adjust and verify
Offer animation scales of 0.5 for shorter transitions. If I agree, first save the original values of window_animation_scale, transition_animation_scale and animator_duration_scale, including whether each was unset, and record how to restore them. This changes how transitions feel, not the TV's hardware speed. Do not clear app data or caches as a routine speed-up step.

When I am ready, reboot and check Home, Projectivy, Inputs, sound and my regular apps. Repeat the three snapshot commands under similar idle conditions and save separate after files.

FINISH WITH
- A short before/after table of available memory figures and package changes. Memory snapshots fluctuate; do not present them as proof of a speed increase.
- The packages changed, with a plain-language explanation.
- A local rollback script for this computer and its exact run command. Restore only this session's recorded changes, including animation and Home settings where changed. Do not run it unless I ask.

Keep logs and connection details local. Exclude pairing codes, addresses and device identifiers from public files.

Begin by checking ADB and confirming the device connection.
